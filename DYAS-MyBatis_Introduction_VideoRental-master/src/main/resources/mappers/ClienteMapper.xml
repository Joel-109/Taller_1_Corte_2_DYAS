<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="edu.unisabana.dyas.sampleprj.dao.mybatis.mappers.ClienteMapper">
  <select parameterType="map" id="consultarClientes" resultMap="ClienteResult" >
    select
    c.nombre,
    c.documento,
    c.telefono,
    c.direccion,
    c.email,
    c.vetado,
    ir.id AS ir_id,
    ir.CLIENTES_documento AS ir_clientes_doc,
    ir.ITEMS_id AS ir_items_id,
    strftime('%Y-%m-%d %H:%M:%S', ir.fechainiciorenta) AS ir_fechainiciorenta,
    strftime('%Y-%m-%d %H:%M:%S', ir.fechafinrenta) AS ir_fechafinrenta,
    i.id AS i_id,
    i.nombre AS i_nombre,
    i.descripcion AS i_descripcion,
    i.fechalanzamiento AS i_fechalanzamiento,
    i.tarifaxdia AS i_tarifaxdia,
    i.formatorenta AS i_formatorenta,
    i.genero AS i_genero,        
    ti.id AS ti_id,
    ti.descripcion as ti_descripcion 
    FROM VI_CLIENTES as c 
    left join VI_ITEMRENTADO as ir on c.documento=ir.CLIENTES_documento 
    left join VI_ITEMS as i on ir.ITEMS_id=i.id 
    left join VI_TIPOITEM as ti on i.TIPOITEM_id=ti.id
  </select>  	

  <select parameterType="map" id="consultarClienteDocumento" resultMap="ClienteResult" >
    select
    c.nombre,
    c.documento,
    c.telefono,
    c.direccion,
    c.email,
    c.vetado,
    ir.id AS ir_id,
    ir.CLIENTES_documento AS ir_clientes_doc,
    ir.ITEMS_id AS ir_items_id,
    strftime('%Y-%m-%d %H:%M:%S', ir.fechainiciorenta) AS ir_fechainiciorenta,
    strftime('%Y-%m-%d %H:%M:%S', ir.fechafinrenta) AS ir_fechafinrenta,
    i.id AS i_id,
    i.nombre AS i_nombre,
    i.descripcion AS i_descripcion,
    i.fechalanzamiento AS i_fechalanzamiento,
    i.tarifaxdia AS i_tarifaxdia,
    i.formatorenta AS i_formatorenta,
    i.genero AS i_genero,        
    ti.id AS ti_id,
    ti.descripcion as ti_descripcion 
    FROM VI_CLIENTES as c 
    left join VI_ITEMRENTADO as ir on c.documento=ir.CLIENTES_documento 
    left join VI_ITEMS as i on ir.ITEMS_id=i.id 
    left join VI_TIPOITEM as ti on i.TIPOITEM_id=ti.id
    WHERE c.documento == #{idcli}
  </select>

  <insert parameterType='map' id='agregarItemRentadoACliente'> 
    insert into VI_ITEMRENTADO (CLIENTES_documento, ITEMS_id, fechainiciorenta, fechafinrenta) 
    VALUES (#{idcli}, #{iditcli}, #{fechainiciocli}, #{fechafincli})
  
  </insert>
    
  <resultMap type='Cliente' id='ClienteResult'>
      <id property='documento' column='documento'/>
      <result property='nombre' column='nombre'/>
      <result property='telefono' column='telefono'/>
      <result property='direccion' column='direccion'/>
      <result property='email' column='email'/>
      <result property='vetado' column='vetado'/>

      <collection property='rentados' resultMap='ItemRentadoResult' columnPrefix='ir_'/>
  </resultMap>

  <resultMap type='ItemRentado' id='ItemRentadoResult'>
      <id property='id' column='id'/>
      <result property='fechainiciorenta' column='fechainiciorenta' />
      <result property='fechafinrenta' column='fechafinrenta' />
      <association property="item" resultMap='edu.unisabana.dyas.sampleprj.dao.mybatis.mappers.ItemMapper.ItemResult' columnPrefix='i_'/>  
  </resultMap> 
</mapper>

<!--

-->



